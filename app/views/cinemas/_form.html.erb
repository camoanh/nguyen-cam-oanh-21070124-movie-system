<%= form_with(model: cinema) do |form| %>
<% if cinema.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(cinema.errors.count, "error") %> prohibited this cinema from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% cinema.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :Room, class: label_class %>
          <%= form.number_field :Room, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :Row, class: label_class %>
          <%= form.text_area :Row, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :Number, class: label_class %>
          <%= form.number_field :Number, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if cinema.persisted? %>
<%= link_to "Cancel", cinema, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", cinemas_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
